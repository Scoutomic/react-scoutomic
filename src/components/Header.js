import React from 'react';

import AppBar from 'material-ui/AppBar';

const Header = (props) => {
	return (
		<div>
			<AppBar
				className="app-bar"
				title="Scoutomic" />
		</div>
	);
}

export default Header;
